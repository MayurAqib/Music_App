import 'package:flutter/material.dart';

class BestMusic extends StatelessWidget {
  const BestMusic({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('B E S T  M U S I C'),
      ),
    );
  }
}
